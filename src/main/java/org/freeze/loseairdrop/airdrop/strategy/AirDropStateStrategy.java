package org.freeze.loseairdrop.airdrop.strategy;

import org.freeze.loseairdrop.managers.bossbar.BossDisplayManager;

public interface AirDropStateStrategy {
    void execute(BossDisplayManager bossDisplayManager);
}
